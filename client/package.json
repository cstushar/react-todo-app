{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-redux": "^6.0.0",
    "react-scripts": "2.1.3",
    "redux": "^4.0.1",
    "redux-saga": "^1.0.0",
    "styled-components": "^4.1.3"
  },
  "scripts":{
    "dev":"export SET NODE_OPTIONS=--openssl-legacy-provider",
    "build":"export SET NODE_OPTIONS=--openssl-legacy-provider",
    "start":"export NODE_OPTIONS=--openssl-legacy-provider start",
    "generate":"export SET NODE_OPTIONS=--openssl-legacy-provider "
  },
  "proxy": "http://localhost:5050",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
